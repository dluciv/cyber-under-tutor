name: 'Кибердядька'
description: 'GitHub action to check code styling'
inputs:
  whom-to-greet:  # id of input
    description: 'Кому привет?'
    required: true
    default: 'Я'
outputs:
  time: # id of output
    description: 'Когда привет'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.whom-to-greet }}
